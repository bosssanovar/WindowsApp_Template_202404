<uiwindow:MainWindowBase 
    xmlns:uiwindow="clr-namespace:UiParts.UiWindow" 
    x:Class="UiParts.UiWindow.StartWindow.StartWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:UiParts.UiWindow.StartWindow"
    mc:Ignorable="d"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
    Title="StartWindowView" Height="450" Width="800"
    WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
            <Button Padding="15,3" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Content="簡単新規作成" Command="{Binding EasyStartCommand}"/>
            <Button Padding="15,3" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Content="新規作成" Command="{Binding StartCommand}"/>
        </StackPanel>
    </Grid>
</uiwindow:MainWindowBase>
